```java
class Solution {
    public double solution(int[] numbers) {
        double answer = 0;
        int tmp = 0;
        tmp = numbers.length;
        for(int i = 0; i < numbers.length; i++) {
            answer += (double)numbers[i];
        }
        answer /= tmp;
        return answer;
    }
}
```
