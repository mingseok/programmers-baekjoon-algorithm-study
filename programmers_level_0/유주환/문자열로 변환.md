```java
class Solution {
    public String solution(int n) {
        String answer = Integer.toString(n);
        return answer;
    }
}

// 숫자 + 부호 식으로 만들어질 경우 유니코드 값을 변환해 처리하는 경우가 생길 수 도 있다. 알고있자!!
```